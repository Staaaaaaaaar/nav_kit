# 2D SLAM 建图：laserscan + interface + slam_toolbox
# TF：odom→base 由 interface；map→odom 由 slam_toolbox
profile: quadrover
use_sim_time: true
map: maps/slam
rviz_config: slam_mapping.rviz
modules:
  static_tf: static_tf.yaml
  laserscan: laserscan.yaml
  interface: interface_mapping.yaml
  slam: slam_mapping.yaml
