# 未知地图导航：边建图边定位边导航
# TF：odom→base 由 interface；map→odom 由 slam_toolbox（无 AMCL）
profile: quadrover
use_sim_time: true
rviz_config: nav_unknown_map.rviz
modules:
  static_tf: static_tf.yaml
  laserscan: laserscan.yaml
  interface: interface_mapping.yaml
  slam: slam_mapping.yaml
  nav2: nav2_indoor.yaml
